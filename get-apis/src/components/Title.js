import React from "react";
import { Grid, Typography } from "@material-ui/core";
function Title() {
    return (
        <Grid item>
         <Typography variant="h3" style={{color:"white"}}> Get 3 API </Typography>
        </Grid>
    );
}
export default Title;